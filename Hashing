#include<stdio.h>
#include<stdlib.h>
#define TABLE_SIZE10

int nSIZE_TABLE=[NULL];
insert void ()
{
	int key index;flag=0,hkey;
	printf("\nEnter a value to insert into hash table\n");
	scanf("\%d",&key);
	hkey=key% (TABLE_SIZE)
	for(i=0;i<TABLE_SIZE;I++)
	{
		INDEX=(hkey+i)%TABLE_SIZE
		if(n[index]==NULL)
		{
			h(index)=key;
			break;
		}
   }
